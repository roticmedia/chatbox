const Goftino = require("./Goftino")

const handleThirdParty = () => {
    try {
        window.addEventListener("goftino-ready", () => {

        })
    } catch (err) {
        console.log(err)
    }
}